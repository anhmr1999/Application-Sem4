<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@drawable/bg_multiple_color"
    tools:context=".MainActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_margin="50dp"
        android:orientation="vertical"
        >

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="25dp"
            android:background="@drawable/common_bg_button"
            android:layout_marginBottom="8dp"
            android:text="Edit Profile"
            android:onClick="SettingChangeProfile"/>

        <Button
            android:id="@+id/MusicSetting"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="25dp"
            android:background="@drawable/common_bg_button"
            android:layout_marginBottom="8dp"
            android:text="Music: ON"
            android:onClick="SettingChangeMusic"/>

        <Button
            android:id="@+id/LanguageSetting"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="25dp"
            android:background="@drawable/common_bg_button"
            android:layout_marginBottom="8dp"
            android:text="language: En"
            android:onClick="SettingChangeLanguage"/>

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="25dp"
            android:background="@drawable/common_bg_button"
            android:layout_marginBottom="8dp"
            android:text="Logout"
            android:onClick="Logout"/>

    </LinearLayout>

    <ImageView
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_marginTop="20dp"
        android:layout_marginLeft="20dp"
        android:src="@drawable/back"
        android:onClick="backToMain"/>

</RelativeLayout>